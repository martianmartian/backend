
<!-- check browser page -->

<style>
  #ghostThree{
    display: none;
    background-color: rgb(149, 237, 207);
    position: absolute;
    height:900px;
    top: 0;
    z-index: 100000;
    font-size: 342%%;
    width: 100%;
  }
</style>

  <div id="ghostThree">
    <div id="ghostThreeP"><p>
      Currently Langtime <b> tutoring session </b>can only be used with <b>Chrome</b> based <b>Computer</b> browsers. Please switch, or download here:
    </p></div>
    <div id="ghostThreeA">
      <a href="http://1drv.ms/1DdpaPv">Chrome (from repo,for Mac only)</a><br>
      <a href="https://support.google.com/chrome/answer/95346?hl=en">Chrome (from Google)</a><br><br>
      <a href="">I will do it Later</a>
    </div>
  </div>

  <!-- check browser -->
  <!-- also check mobile hereerere -->
<script>
  $(document).ready(function(){
    var brParser = new UAParser();
    var browserName=brParser.getBrowser();
    var browserDevice=brParser.getDevice();

    var invalidUser = (browserName.name != 'Chrome'  || parseInt(browserName.major)<=38 || browserDevice.type=="mobile") ? true:false;

    if (invalidUser) {
      $("#newSession").off("click");
      $("#newSession").click(function(){
        $("#ghostThree").show()
      })
    };

    $("#ghostThree").click(function(){
      $("#ghostThree").hide();
    })



  })
</script>
